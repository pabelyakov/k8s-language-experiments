meta {
  name: Create User Invalid Name
  type: http
  seq: 2
}

post {
  url: {{baseUrl}}/v1/users
  body: json
  auth: none
}

headers {
  Content-Type: application/json
}

body:json {
  {
    "name": ""
  }
}

tests {
  test("status is 400", function () {
    expect(res.getStatus()).to.equal(400);
  });
}

docs {
  Negative case: empty name should be rejected.
}
