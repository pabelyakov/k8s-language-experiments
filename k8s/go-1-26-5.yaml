apiVersion: apps/v1
kind: Deployment
metadata:
  name: beer-vote-go-1-26-5
  labels:
    app: beer-vote
    runtime: go-1-26-5
spec:
  replicas: 1
  selector:
    matchLabels:
      app: beer-vote
      runtime: go-1-26-5
  template:
    metadata:
      labels:
        app: beer-vote
        runtime: go-1-26-5
    spec:
      containers:
        - name: beer-vote
          image: ghcr.io/pabelyakov/beer-vote:go-1-26-5
          imagePullPolicy: IfNotPresent
          ports:
            - name: http
              containerPort: 8080
          resources:
            requests:
              cpu: "2"
              memory: 2Gi
            limits:
              cpu: "2"
              memory: 2Gi
          readinessProbe:
            httpGet:
              path: /health
              port: http
            initialDelaySeconds: 3
            periodSeconds: 5
          livenessProbe:
            httpGet:
              path: /health
              port: http
            initialDelaySeconds: 10
            periodSeconds: 10
---
apiVersion: v1
kind: Service
metadata:
  name: beer-vote-go-1-26-5
  labels:
    app: beer-vote
    runtime: go-1-26-5
spec:
  type: NodePort
  selector:
    app: beer-vote
    runtime: go-1-26-5
  ports:
    - name: http
      port: 8080
      targetPort: http
      nodePort: 30011
